<Layouts.home current_scope={@current_scope} contact_name={@contact_name}>
  <p>Welcome back, {@current_scope.user.email}!</p>

  <.invoice_summary new={@counts.new} overdue={@counts.overdue} total={@counts.total} />

  <:cta>
    <.button variant="hero-cta" navigate={~p"/invoice"}>
      View Invoices
    </.button>
  </:cta>
</Layouts.home>
